The above file consists of the codes and it just have to be opened for the program to be run after the below steps.


Run the following command in command prompt (or) powershell.

npm create vite@latest et-saathi -- --template react ->It is used to create a react project

cd et-saathi
  npm install  ->These commands are used to install the dependencies

npm run dev ->This runs the coding


